{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -q '\\.py$' && \"$CLAUDE_PROJECT_DIR/.venv/bin/ruff\" check \"$f\" --output-format=concise 2>&1 || true; }",
            "statusMessage": "Checking lint..."
          }
        ]
      }
    ]
  }
}
